# AI Chatbot using OpenAI GPT-3.5

A simple Python chatbot that uses OpenAI’s GPT-3.5 to generate interactive responses. Built with the latest OpenAI SDK, it runs in the terminal and allows seamless conversation with easy exit commands. Perfect as a starting point for AI projects or portfolio demos.

## Usage

1. Install dependencies:
   ```bash
   pip install openai
